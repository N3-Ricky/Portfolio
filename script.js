// For Top Nav Bar Animation

function toggleMenu() {
    const menu = document.querySelector(".menu-links")
    const icon = document.querySelector(".burger-icon")
    menu.classList.toggle("open")
    icon.classList.toggle("open")
}